import VueApexCharts from 'vue-apexcharts'

export default {
    props: {
        chartData: Object,
    },
    data() {
        return {
        }
    },
    components: {
        VueApexCharts, 
    }
}